// Frontend entrypoint placeholder.
import React from "react";
import { createRoot } from "react-dom/client";

const App: React.FC = () => <div>OORCA</div>;

const root = createRoot(document.getElementById("root")!);
root.render(<App />);